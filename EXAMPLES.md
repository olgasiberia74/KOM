# Примеры задач для AI

### Сценарий 1: Добавление новой функциональности
**Промпт:** "Добавь возможность удалять задачу по её ID. Измени `core/manager.py`. Следуй правилам из `AI_GUIDELINES.md`."
**Ожидаемый результат:** Появление метода `delete_task(task_id: int)`, проверка существования ID, обновление JSON-файла.

### Сценарий 2: Исправление бага
**Промпт:** "Исправь ошибку: при пустом файле задач программа вылетает с `JSONDecodeError`. Модифицируй `storage/json_handler.py`."
**Ожидаемый результат:** Добавление блока `try-except` и создание пустого списка задач при отсутствии файла.

### Сценарий 3: Рефакторинг
**Промпт:** "Проведи рефакторинг метода `save_tasks` в `storage/json_handler.py`, чтобы использовать контекстный менеджер `with open()`. Соблюдай PEP 8."
**Ожидаемый результат:** Оптимизация работы с файлом, исключение утечек памяти, чистый код.
